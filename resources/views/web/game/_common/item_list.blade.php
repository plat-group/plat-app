<div class="box-game-item position-relative">
    <div class="thumb-item mb-2">
        <img src="{{ $item->thumb_url }}" alt="{{ $item->name }}" class="w-100"/>
    </div>
    <h5 class="">
        <a href="#" title="{{ $item->name }}" class="stretched-link">
            {{ $item->name }}
        </a>
    </h5>
</div>
